kind = "web"
previewPath = "/"
title = "Shree Overseas Education"
version = "1.0.0"
id = "artifacts/shree-overseas"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24778

[services.development]
run = "pnpm --filter @workspace/shree-overseas run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/shree-overseas", "run", "build" ]
publicDir = "artifacts/shree-overseas/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24778"
BASE_PATH = "/"
